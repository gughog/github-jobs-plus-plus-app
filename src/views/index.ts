// Here it will export all (and only) views.
import {HomeScreen} from './Home';
import {SearchScreen} from './SearchScreen';
import {FavoritesScreen} from './Favorites';

export {HomeScreen, SearchScreen, FavoritesScreen};
